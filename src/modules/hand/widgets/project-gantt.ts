/**
 * Widget boundary for the project timeline. The implementation keeps its
 * legacy view path for compatibility until the SVG renderer is split into
 * smaller widgets.
 */
export { ProjectGantt } from "../views/project-gantt";

