/**
 * Stable public entry point for presentation primitives.
 *
 * The legacy `ui.tsx` and `controls.tsx` entry points remain available while
 * feature code moves to the explicit component/widget/section vocabulary.
 */
export {
  Button,
  Card,
  Checkbox,
  Field,
  IconButton,
  Input,
  Select,
  Switch,
  Textarea,
} from "../controls";
export {
  Alert,
  Badge,
  DescriptionList,
  Dialog,
  EmptyState,
  LoadingState,
  ProgressBar,
  Tabs,
} from "../ui";

